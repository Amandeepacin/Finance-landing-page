// src/components/ContentSection.tsx

import React from 'react';

const ContentSection: React.FC = () => {
  return (
    <div className="content-section">
      {/* Add the static content based on the design */}
    </div>
  );
};

export default ContentSection;
